server:
  port: 9093

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micro_hotels
    username: krsna
    password: Krsna@108

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true

  application:
    name: HOTEL-SERVICE

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

  instance:
    prefer-ip-address: true
#    fetch-registry: true
#    register-with-eureka: true