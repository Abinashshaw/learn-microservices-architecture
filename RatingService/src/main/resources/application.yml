server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micro_app
    username: root
    password: Krsna@108

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  application:
    name: RATING-SERVICE

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true